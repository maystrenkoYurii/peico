.loadable-not-found {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  padding: var(--unitSpacing);

  &_404 {
    font-size: 36px;
    margin-bottom: var(--unitSpacing);
  }

  &_text {
    font-size: 21px;
  }
}
