.footer-action {
  flex: 1;
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  color: var(--white);

  &_element {
    margin-top: calc(var(--unitSpacing) / 2);
    margin-left: var(--unitSpacing);
    margin-right: var(--unitSpacing);
    text-align: center;
  }
}
